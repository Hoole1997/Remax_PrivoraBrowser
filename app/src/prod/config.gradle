ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-5794989166107987~5646989638", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "ca-app-pub-5794989166107987/8300755908",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-5794989166107987/8492327592", // 插页广告测试ID
                             splash      : "ca-app-pub-5794989166107987/5922994253",       // 开屏广告测试ID
                             native      : "ca-app-pub-5794989166107987/6282921055",        // 原生广告测试ID
                             full_native : "ca-app-pub-5794989166107987/4790406217",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（官方测试 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/Privora_IV",
                       splash      : "/23312177113/Privora_SP",
                       native      : "",
                       full_native : "",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8831396", // Pangle测试应用ID
              adUnitIds    : [splash      : "890156049",        // 开屏广告测试ID（竖屏）
                              banner      : "983195593",        // 横幅广告测试ID（320x50）
                              interstitial: "983195591",       // 插页广告测试ID（竖屏）
                              native      : "983195603",        // 原生广告测试ID
                              full_native : "983195594",        // 全屏原生广告测试ID
                              rewarded    : ""         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1hcmap0oebqfu",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "aca442fe3ca3e48df5e11e4663d2884d8",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1hcmap0qm32sh",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "",  // 激励广告位 ID
                             native      : "n1hcmap0rcbm8a",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1hcmap0rnlnm6",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "n1hcmap0s3lou2",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : "n1hcmap0t17380"]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.private.secure.download.videodownload.browser.free",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 1,
           versionName  : "1.0.0"]

    url = [fcmUrl    : "https://summer.gravitonlumina.com",
           fcmPkg   : "com.private.secure.download.videodownload.browser.free"]

    // 统计归因配置
    analytics = [adjustAppToken: "43f6r2r3nolc",           // Adjust App Token
                 thinkingDataAppId: "b40e52e6f62d46529ddbd5d5f159c485",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "https://summer.gravitonlumina.com",     // 数数上报域名
                 defaultUserChannel: "paid"]                           // 默认用户渠道，internal默认paid
}